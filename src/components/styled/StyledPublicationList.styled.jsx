import styled from "styled-components";

const StyledPublicationList = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1.5rem;
`;

export default StyledPublicationList;
