import styled from "styled-components";

const StyledPublicationDetails = styled.main`
  inline-size: max(288px, 44.375%);
  margin-inline: auto;

  .author-info {
    margin-block-end: 2rem;
  }
`;

export default StyledPublicationDetails;
